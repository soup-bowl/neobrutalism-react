export const colorMap: Record<string, string> = {
	primary: "#fec2a9",
	success: "#4ade80",
	warning: "#fde047",
	danger: "#f87171",
}

export const boxy = (size: string) =>
	`${size}px ${size}px 0px rgb(0, 0, 0), ${size}px ${size}px 0px rgb(0, 0, 0), ${size}px ${size}px 0px rgb(0, 0, 0)`